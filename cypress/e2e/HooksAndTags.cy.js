describe("hooks/tags", () => {

    //HOOKS: before(), after(), beforeEach(), afterEach()
    //TAGS: skip, only

    before(() => {
        cy.log("************************************* Launch App *************************************")
    })

    after(() => {
        cy.log("************************************* Close App *************************************")
    })

    beforeEach(() => {
        cy.log("************************************* Login *************************************")
    })

    afterEach(() => {
        cy.log("************************************* Logout *************************************")
    })


    it.skip("", () => {

    })


    it.only("", () => {

    })


    it("", () => {

    })
})